import { Meta } from '@storybook/addon-docs';

<Meta title="Components/Buttons/Buttons Docs" />

<h1 className="mb-32">
  <u>Buttons</u>
</h1>

<h2>Description </h2>
<p className="fs-16">
  A component that renders a buttons extended by <a href="https://react-bootstrap.github.io/">react-bootstrap</a>. On the examples you can
  see differents combinations to differents buttons styles with multiples purposes.
</p>
<p className="fs-16">Check all props available in each example on Docs tab.</p>

<h2>Component usage</h2>

```tsx
import { Button } from 'ama-react-design-system/button';

const args = {
  className: 'shadow-none',
  children: 'Default',
  variant: 'brand-green-main',
  size: 'lg'
};

<Button {...args} />;
```
