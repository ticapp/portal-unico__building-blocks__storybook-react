import { Meta } from '@storybook/addon-docs';

<Meta title="Components/Horizontal Menu/Horizontal Menu Docs" />

<h1 className="mb-32">
  <u>Horizontal Menu</u>
</h1>

<h2>Description</h2>

<p className="fs-16">Render the main navigation menu.</p>
<p className="fs-16">Check all props available on Docs tab.</p>

<h2>Component usage</h2>

```tsx
import { HorizontalMenu } from '@ama-react-design-system/horizontal-menu';

const links: HorizontalMenuLink[] = [
  {
    id: '1',
    active: true,
    label: 'Serviços',
    link: '/services'
  },
  {
    id: '2',
    active: false,
    label: 'Entidades',
    link: '/entities'
  },
  {
    id: '3',
    active: false,
    label: 'Atendimento',
    link: '/attendance'
  },
  {
    id: '4',
    active: false,
    label: 'Notícias',
    link: '/news'
  }
];

<HorizontalMenu links={links} />;
```
