import { Meta } from '@storybook/addon-docs';

<Meta title="Components/User Area/User Area Docs" />

<h2>Description</h2>

<p>
  Renders the UserArea dropdown menu
</p>

<h2>Component usage</h2>

```jsx
const options = [
  {
    authenticatedOption: false,
    link: '/login',
    icon: 'ama-login',
    label: 'Entrar no portal',
  },
  {
    authenticatedOption: false,
    link: '/register',
    icon: 'ama-add-user',
    label: 'Criar registo',
  },

  {
    authenticatedOption: true,
    link: '/user-area',
    icon: 'ama-user',
    label: 'Area Reservada',
  },
  {
    authenticatedOption: true,
    link: 'logout',
    icon: 'ama-logout',
    label: 'Terminar sessão',
  },
] as UserAreaOption[];

return (
  <BrowserRouter>
    <Container>
      <Row className="d-flex justify-content-center">
        <Col xs={4}>
          <UserArea
            options={options}
            isAuthenticated={true}
            label={'John Doe'}
          />
        </Col>
      </Row>
    </Container>
  </BrowserRouter>
);
```
