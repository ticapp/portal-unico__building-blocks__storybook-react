import { Meta } from '@storybook/addon-docs';

<Meta title="Components/User Area/User Area Docs" />

<h1 className="mb-32">
  <u>User Area</u>
</h1>

<h2>Description</h2>

<p className="fs-16">Renders the UserArea dropdown menu.</p>
<p className="fs-16">Check all props available in each example on Docs tab.</p>

<h2>Component usage</h2>

```tsx
import { UserArea } from '@ama/react-design-system';

const options = [
  {
    authenticatedOption: false,
    link: '/login',
    icon: 'ama-login',
    label: 'Entrar no portal'
  },
  {
    authenticatedOption: false,
    link: '/register',
    icon: 'ama-add-user',
    label: 'Criar registo'
  },

  {
    authenticatedOption: true,
    link: '/user-area',
    icon: 'ama-user',
    label: 'Area Reservada'
  },
  {
    authenticatedOption: true,
    link: 'logout',
    icon: 'ama-logout',
    label: 'Terminar sessão'
  }
] as UserAreaOption[];

return (
  <BrowserRouter>
    <Container>
      <Row className="d-flex justify-content-center">
        <Col xs={4}>
          <UserArea options={options} isAuthenticated={true} label={'John Doe'} />
        </Col>
      </Row>
    </Container>
  </BrowserRouter>
);
```
