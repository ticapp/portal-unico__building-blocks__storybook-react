import { Meta } from '@storybook/addon-docs';

<Meta title="Components/StatusBar/StatusBar Docs" />

<h1 className="mb-32">
  <u>StatusBar</u>
</h1>

<h2>Description</h2>

<p className="fs-16">Renders the Stats bar component.</p>
<p className="fs-16">Check all props available in each example on Docs tab.</p>

<h2>Component usage</h2>

```tsx
import { StatusBar } from '@ama-react-design-system/status-bar';

const args = {
  url: 'https://www.google.com',
  title: 'Pontos da carta de condução',
  subtitle: '15 pontos',
  icon: 'ama-badge',
  backButtonText: 'Voltar ao início'
};

return (
  <BrowserRouter>
    <StatusBar {...args} />
  </BrowserRouter>
);
```
