import { ScrollTop } from './scroll-top';

<h2>Description</h2>

<p>Renders a button at page bottom right. On click will scroll page to the top.</p> 

<p>Since it uses css postion fixed, consider using the component at the start of the DOM tree</p>

### Component usage
```js
<ScrollTop />
```